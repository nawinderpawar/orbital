# Changelog

## 0.1.0

- Initial release
- Sidebar TreeView with repo status
- Webview Dashboard with card layout and inline notes
- Git status: branch, ahead/behind, dirty files, worktrees, last commit
- Periodic polling (configurable interval)
- Persistent storage in `~/.orbital/data.json`
- Commands: add/remove repos, edit notes, set alias, open folder/terminal
